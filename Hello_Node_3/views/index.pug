extends layout

block content
  h1 !{title}
  p 반갑습니다 !{title}
  ul  
    li  
      a(href="/") 안녕하세요 오늘은 #{date} 입니다.

      form(method="POST",action="/")
      input(type="text",name="m_user")
      button 전송

